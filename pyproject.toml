[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "bwt_walmart_api_doc"
authors = [{name = "Graziella", email = "graziella@lumache"}]
dynamic = ["version", "description"]
